#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a = 48;

	while (a <= 57)
	{
		putchar(a);
		a++;
	}
	int b = 65;

	while (b <= 70)
	{
		putchar (b);
		b++;
	}
	{
		putchar('\n');
	}
	return (0);
}
