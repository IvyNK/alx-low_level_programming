#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int putchar(int a);
	int a = 97;

	while (a < 123)
	{
		putchar(a);
		a++;
	}
	return (0);
}
